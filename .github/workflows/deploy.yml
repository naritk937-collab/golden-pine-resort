name: Deploy to Golden Pine Resort Server

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to dev.goldenpineresorts.com via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: dev.goldenpineresorts.com
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
<<<<<<< HEAD
          protocol: sftp
          port: 22
          local-dir: ./
          server-dir: /public_html/
=======
          protocol: ftp
          port: 21
          
          # ✅ แก้ไข Path ต้นทาง: ต้องมี / ปิดท้ายเพื่อให้ Action รู้ว่าเป็นโฟลเดอร์
          local-dir: ./golden-pine-resort-main/
          
          # ✅ แก้ไข Path ปลายทาง: ชี้ไปที่ Root ของ Subdomain ใน Plesk
          server-dir: /dev.goldenpineresorts.com
          
>>>>>>> 33313bf17379d9a3f79d1287f949ba809704e0a3
          dangerous-clean-slate: false
          timeout: 60000
          log-level: verbose
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            .github/
            README.md
            .gitignore
            sync-to-server.ps1
            sync.bat
